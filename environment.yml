name: audio2map
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - ffmpeg
  - pip
  - pip:
      - librosa>=0.10.0
      - soundfile>=0.12.0
      - requests>=2.28.0
      - tqdm>=4.66.0
      - rosu-pp-py>=4.0.0
      - torch>=2.1
      - -e .
