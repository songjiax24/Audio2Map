# Copy to `.env` (gitignored). Do not commit real disk paths or contact details.

AUDIO2MAP_DATA_ROOT=/path/to/audio2map_data

# Collector identity — set locally if Sayobot requires a contact; leave unset otherwise.
# AUDIO2MAP_CONTACT=
SAYOBOT_REFERER=https://osu.sayobot.cn/
# SAYOBOT_USER_AGENT=
