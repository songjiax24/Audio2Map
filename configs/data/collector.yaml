# Data collection defaults — loaded via: python -m scripts.collect.cli --config configs/data/collector.yaml
# List API is T=2 (new) C=1 (Ranked & Approved); mania 4K via v2 info then mini extract.

page_size: 50
list_delay: 1.0
