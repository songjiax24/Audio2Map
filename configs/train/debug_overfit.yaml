# Debug overfit on a single chart (see scripts/train_debug_overfit.py)

d_model: 256
n_heads: 4
encoder_layers: 4
decoder_layers: 4
dropout: 0.0

lr: 0.0003
steps: 500
batch_size: 1
log_every: 50

window_bars: 16

# Example chart with pre-offset events
# osu: $AUDIO2MAP_DATA_ROOT/raw/<set_id>/<chart>.osu
# start_bar: -1
